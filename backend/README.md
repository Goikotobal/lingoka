# Lingoka Backend

FastAPI backend with multi-agent LangGraph orchestration.

## Setup

```bash
# Create virtual environment
python3 -m venv venv
source venv/bin/activate

# Install dependencies
pip install -r requirements.txt

# Run development server
uvicorn main:app --reload
```

See `/docs/LINGOKA_SETUP.md` for full setup instructions.
